{% extends 'app.html.twig' %}


{% block body %}
    <div class="container mt-3">
        <div class="card">
            <div class="px-3 py-3">
                <h3 class="display-3">Formulaire détails</h3>
                <p>Merci d'indiquer ci dessous quelques renseignements sur toi</p>

                {{ form_start(form, {'attr': {'class': 'form-signin'}}) }}

                    {{ form_label(form.telephone) }}
                    {{ form_widget(form.telephone) }}
                <div class="mt-3">
                    {{ form_label(form.ville) }}
                    {{ form_widget(form.ville) }}
                </div>
                <div class="mt-3">
                    {{ form_label(form.codePostal) }}
                    {{ form_widget(form.codePostal) }}
                </div>
                <button class="mt-3 btn btn-success">Suivant</button>
                {{ form_end(form) }}
            </div>
        </div>
    </div>
{% endblock %}
